import React from 'react';
import { useQuery } from 'react-query';
import Loading from '../Shared/Loading/Loading';

const MyHistory = () => {
	return (
		<div>
			<h2 className='text-center text-secondary text-xl my-2'>My Treatment History</h2>
		</div>
	);
};

export default MyHistory;